def solution(n):
    # 계속 2로 나누면서 진행 나누어 떨이질 때까지, 홀수면 -1
    # 2진수 변환 시
    # 1. 2로 나누기 = 끝값 버리기
    # 2. 끝값이 1이면 0으로 바꾸고 (에너지 소비) 계속 버리면서 진행 => 0이 될때 까지
    # 즉, 1의 개수 가 에너지 소모량
     return bin(n).count('1')